stage: Development

projects-api:
  cors:
    allowed-origins: http://localhost:3000,http://localhost:3001
  portfolio:
    api:
      url: http://localhost:3000/api
      secret: revalidate-secret
  jwt:
    secret: secret-key

spring:
  datasource:
    username: projects-api-user
    url: jdbc:postgresql://localhost:5432/projects-api-db
    password: projects-api-password
  jpa:
    show-sql: true
